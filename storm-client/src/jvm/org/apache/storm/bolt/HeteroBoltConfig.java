package org.apache.storm.bolt;

import org.apache.storm.daemon.worker.WorkerState;

import java.util.concurrent.locks.Lock;

public class HeteroBoltConfig {
    /*
    WorkerState workerStateHook;

    private static HeteroBoltConfig heteroBoltConfigSingleton = null;

    private HeteroBoltConfig(WorkerState _workerStateHook) {
        workerStateHook = _workerStateHook;

    }

    public static HeteroBoltConfig get() {
        if (heteroBoltConfigSingleton == null) {
            Lock
            heteroBoltConfigSingleton
        }
    }
    */
}
